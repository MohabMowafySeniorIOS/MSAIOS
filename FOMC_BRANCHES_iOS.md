# iOS — اجتماعات الفيدرالي + الفروع

الميزتين اتدمجوا **جوّه المشروع** بنفس الـ architecture الموجودة:
UIKit + storyboard للتنقّل، شاشات SwiftUI متدفوعة بـ `UIHostingController`،
`hostName` + `URLSession`، و`.localized` للنصوص — زي `ContentAPI.swift`
و`SettingsView.swift` بالظبط.

## ملفات جديدة (٧)

```
MVP/Scenes/Auth/
├── FomcAPI.swift                    نموذج الاجتماع + FomcStore (كاش ١٥ دقيقة)
└── BranchAPI.swift                  نموذج الفرع + BranchStore + UserLocationProvider

MVP/Scenes/MetalsScenes/
├── FomcFormatters.swift             MSAFormat — التواريخ/العدّاد/الفائدة/المسافة
├── FomcView.swift                   التقويم + التفاصيل + كارت الرئيسية
├── BranchesView.swift               القايمة + التفاصيل بخريطة + الأفعال
└── MoreVC+MSAExtraRows.swift        صفّين جداد في «المزيد»

MVP/Scenes/HomeFiles/
└── HomeVC+FomcCard.swift            كارت الاجتماع القادم في تذييل جدول الرئيسية
```

كلهم **مضافين في `MSA.xcodeproj` على الـ target** — مش محتاج تسحبهم في
Xcode. (٤ مداخل لكل ملف: BuildFile + FileReference + عضوية المجموعة +
مرحلة Sources.)

## ملفات معدّلة (٥)

| الملف | التعديل |
|---|---|
| `MoreVC.swift` | سطر واحد: `installMSAExtraRows()` في `viewDidLoad` |
| `HomeVC.swift` | سطر واحد: `installFomcCard()` بعد إعداد الجدول |
| `NotificationSettings.swift` | نوع إشعار جديد `.fomc` → توبيكس `fomc_ar` / `fomc_en` |
| `LocalizationFiles/{ar,en}.lproj/Localizable.strings` | ٥٤ مفتاح جديد، متطابقين في اللغتين |
| `Core/Info.plist` | وصف إذن الموقع — شوف تحت |

**مفيش أي تعديل على `Home.storyboard`.** الصفّين الجداد بيتبنوا بالكود:
الملف ٤٧٥ كيلوبايت وفيه ٤٤ stackView متداخلة، وتعديل XML بالحجم ده كان
هيبقى هش وصعب تراجعه. الكود بيلاقي حاوية الصفوف من الـ outlet الموجود
(`AboutView`) ويبني صفوف بنفس شكل الموجود. ولو الشاشة اتغيّرت بعدين
ومالقاش الحاوية، بيسجّل تحذير ويخرج — الشاشة تفضل شغالة، مفيش crash.

## ⚠️ حاجتين لازم تعملهم

### ١. `hostName` واقف على localhost

```swift
// MVP/LocalizationFiles/Constant.swift:26
let hostName = "http://127.0.0.1:8000/api/v1/"
```

الشاشتين الجداد (زي `content` و`bullions` الموجودين) هيحاولوا يوصلوا
لـ`127.0.0.1` على الجهاز نفسه ويفشلوا. خلّيها `https://api.msagold.com/api/v1/`
قبل ما تختبر على جهاز حقيقي أو ترفع build.

**سيبتها زي ما هي** لأنها إعداد تطوير مقصود عندك ومؤثرة على كل الميزات
مش الجداد بس — القرار قرارك.

### ٢. وصف إذن الموقع في `Info.plist`

النص القديم كان بتاع تطبيق تاني خالص:

> «We need your location so that the **Recovery** can reach you to serve you»

غيّرته لـ:

> «نستخدم موقعك لعرض أقرب فروعنا إليك وترتيبها حسب المسافة.»

**وشلت `NSLocationAlwaysUsageDescription` و
`NSLocationAlwaysAndWhenInUseUsageDescription`**: التطبيق عمره ما بيطلب
`Always` (اتأكدت — `CLLocationManager` مستخدم في الفروع بس)، وإعلان إذن
مش بتستخدمه سبب شائع لرفض المراجعة.

## قرارات تستاهل الشرح

**إذن الموقع في شاشة الفروع بس** — مش عند فتح التطبيق. الطلب في سياقه
(«عايز أشوف أقرب فرع») نسبة قبوله أعلى بكتير، وبيوفّر سؤال في المراجعة
عن سبب طلب الموقع من غير مناسبة. والشاشة **شغالة من غير الإذن**: بتعرض
الفروع بترتيب اللوحة مع زرار تفعيل. الرفض بيقلّل فايدة الشاشة مش بيفضّيها.

**حالة الاجتماع بتتحسب على الجهاز مش من رد السيرفر.** الرد ممكن يكون من
الكاش من ساعة، فلو اعتمدنا على `status` اللي جاي فيه كان اجتماع خلص من
ساعة هيفضل «قادم» والعدّاد يمشي بالسالب.

**العدّاد بيرجّع `nil` لما الوقت يعدّي** بدل صفر — الواجهة بتخفيه بالكامل.

**كارت الرئيسية في `tableFooterView` مش كصف في الجدول** — كده مش بنلمس
`numberOfRowsInSection` ولا `cellForRowAt`، فمفيش احتمال نكسر جدول الأسعار.
والكارت بيبلّغ بارتفاعه لما يتحمّل، لأن `tableFooterView` محتاج إطار ثابت
ومش بيعيد القياس لوحده — من غير كده كان هيتقاس وهو فاضي ويفضل مخفي للأبد.

**MapKit مش Google Maps.** جزء من النظام: مفيش مفتاح API ولا تكلفة ولا
pod جديد. (أندرويد محتاج مفتاح Google Maps لأن مفيش بديل من النظام هناك.)

**الخريطة تفاعلها مقفول** (`allowsHitTesting(false)`) — هي جوّه ScrollView
رأسي، ولو سابناها تستقبل السحب كان المستخدم اللي بيحاول يمرّر الشاشة
هيحرّك الخريطة بالغلط. اللي عايز يتحرك بيدوس «الاتجاهات».

**الأرقام دايماً LTR** حتى في الواجهة العربية — من غير كده رقم زي
`+20 100 123 4567` بيتعرض بترتيب مقلوب والمستخدم بينسخه غلط.

**`UserLocationProvider.finish` محمية من النداء مرتين.** وصول نتيجة بعد
انتهاء المهلة كان هيستأنف نفس الـ `continuation` مرتين — وده **crash** في
Swift مش مجرد باج.

**مفيش أي مفاتيح في التطبيق.** بيكلّم سيرفر MSA بس، والسيرفر عنده مفاتيح
FRED وFCM في `.env`.

## اختبار بعد ما تشغّل

| الحالة | المتوقع |
|---|---|
| «المزيد» | صفّين جداد بعد «من نحن»: اجتماعات الفيدرالي، فروعنا |
| الرئيسية | كارت الاجتماع القادم بعدّاد تحت جدول الأسعار |
| افتح الفروع أول مرة | نافذة إذن الموقع تظهر |
| اقبل الإذن | ترتيب بالأقرب + «مرتّبة بالأقرب ليك» + مسافة على كل كارت |
| ارفض الإذن | القايمة كاملة + زرار «افتح الإعدادات» |
| اقفل خدمة الموقع | رسالة «خدمة الموقع مقفولة» — مختلفة عن رسالة الرفض |
| تفاصيل فرع | خريطة بعلامة + «الاتجاهات» يفتح Apple Maps جاهزة |
| زرار الواتساب | يفتح واتساب لو متثبّت، وإلا wa.me في المتصفح |
| بدّل لغة التطبيق | الأسماء والعناوين تتغيّر **من غير طلب شبكة** |
| الإعدادات | سويتش «اجتماعات الفيدرالي» موجود |
| شاشة الفيدرالي | العدّاد بينزل كل ثانية؛ السابقة فيها الفائدة والتغيير |

## ملاحظة جانبية

ملفات الترجمة فيها **٣٠ مفتاح مكرر في الإنجليزي و٣٣ في العربي** —
موجودين من قبل التعديل ده ومش من إضافتي (مفاتيحي الـ٥٤ مفيش فيها تكرار،
اتأكدت). التكرار مش بيكسر حاجة (آخر قيمة هي اللي بتتقرا) بس يستاهل تنضيف
في وقت فاضي.
