# شاشات المتجر في التطبيقين

## اللي اتعمل

| | أندرويد | iOS |
|---|---|---|
| القايمة | `ProductsScreen.kt` | `ShopView.swift` |
| التفاصيل | `ProductDetailsScreen.kt` | `ProductDetailsView` (نفس الملف) |
| السلة والطلب | `CartScreen.kt` | `CartView.swift` |
| طلباتي | `OrdersScreen.kt` | `OrdersView` (نفس الملف) |
| الشبكة | `ShopDtos.kt` + `ShopApiRepository.kt` | `ShopAPI.swift` |

الدخول من **«المزيد»**: صفّين جداد — «سبائك ومشغولات» و«طلباتي» —
بنفس شكل صفوف الفروع والفيدرالي.

## القاعدة الأساسية: التطبيق بيعرض، ما بيحسبش

مفيش أي حتة في التطبيقين بتضرب سعر الجرام × الوزن. السيرفر بيرجّع
`price` جاهز:

```json
"price": {
  "gram_price": 7485.71,
  "metal_value": 74857.14,
  "manufacturing_total": 1200,
  "tax_amount": 0,
  "total": 76057.14,
  "priced": true
}
```

كان ممكن نحسب محلياً من سعر الرئيسية، بس ساعتها كل جهاز بيحسب لوحده
والرقم اللي العميل شافه ممكن يخالف اللي السيرفر سجّله. ومفيش مسار
واحد بيبعت سعر للسيرفر — دي حماية مالية مش تنظيم.

### `priced: false` مهمة

معناها السعر لسه ما وصلش من Firestore. الشاشة بتعرض **«جاري تحميل
السعر»** مش صفر جنيه، وزرار «أضف للسلة» بيتقفل. عرض صفر معناه العميل
هيفتكر إن المنتج ببلاش.

## الفرق اللي العميل لازم يشوفه

كل شاشة فيها إجمالي بتقول **مين حسبه وإمتى**:

- **بعربون** — السعر متثبّت، والكارت بيقول «سعر متثبّت» بالأخضر.
- **عند الاستلام** — الرقم **تقديري**، والكارت بيقول «بيتحسب عند
  الاستلام» بالذهبي، وفي السلة فيه سطر تحذير تحت الإجمالي.

ولو تثبيت السعر انتهى، الطلب بيعرض سطر أحمر بيقول إن السعر هيتحسب من
جديد — ده تغيير جوهري في شروط الطلب.

## قرارات في السلوك

**السلة في السيرفر مش في التطبيق.** كل نداء بيرجّع السلة كاملة
بالإجماليات، فالشاشة بتتحدّث من نفس الرد من غير طلب تاني.

**مفيش كاش للمنتجات.** `BranchApiRepository` بيكاش عشر دقايق لأن
الفروع مبتتغيّرش. هنا السعر بيتحرك مع السوق، والكاش معناه إن العميل
يشوف رقم ويطلب برقم تاني. السيرفر بيكاش ٤٥ ثانية وده كفاية.

**رسايل السيرفر بتوصل بنصّها.** «السعر مش محدّث دلوقتي فمش ممكن
نثبّته» و«المنتج ده خلص من المخزون» رسايل المستخدم محتاج يقراها.
الاتنين بيفكّوا `message` من جسم الـ 422 بدل «حصل خطأ».

**401 مميّز عن باقي الأخطاء.** بيودّي المستخدم لشاشة الدخول بدل ما
يعرض رسالة.

**الفرع بيتختار تلقائياً.** قايمة الفروع بتيجي مرتّبة بالأقرب من
السيرفر (لو الموقع متاح) وأول واحد بيبقى مختار. مش بنطلب إذن الموقع
من السلة — لو المستخدم رفضه قبل كده القايمة بتيجي بترتيب اللوحة.

**الطرق المرفوضة بتتخفي.** المشرف بيقدر يقفل «بعربون» على منتج معيّن،
والسيرفر بيرفض الطلب كله ساعتها — فالشاشة بتخفي الاختيار من الأول بدل
ما المستخدم يملا البيانات وياخد رفض.

**بعد نجاح الطلب** شاشات المتجر والسلة بتتشال من الـ stack والطلب
بيتفتح مفتوح عشان الكود يبان على طول. الرجوع لسلة فاضية كان هيبان كأن
حاجة ضاعت.

## التحقق اللي اتعمل

مفيش Android SDK ولا Xcode في البيئة دي، فالفحص كان ثابت بس على
بيانات حقيقية:

1. **مطابقة العقد** — سكربت بيقرا `@Json(name=)` في كوتلن وحقول
   `struct` في سويفت وبيقارنهم بردود الـ API الحقيقية من سيرفر شغّال.
   **٤٨ كلاس/struct مطابقين حقل بحقل.** الحقل المطلوب اللي مش في الرد
   بيتحسب فشل؛ الاختياري لأ.
2. **الموارد** — كل `R.string` و`R.drawable` في أندرويد وكل
   `"key".localized` في iOS موجودين فعلاً. العربي والإنجليزي **٣٩٤
   نص في الاتنين، صفر اختلاف**.
3. **`project.pbxproj`** — اتفحص بمحلّل OpenStep بيقلّد قواعد
   nanaimo. **٥٧٨ كائن، سليم.** واتأكدنا إن المحلّل فعلاً بيمسك
   المشكلة: نسخة مكسورة عن قصد رجّعت نفس خطأ CocoaPods بالحرف.
4. **التوازن والمراجع** — كل ملف جديد متوازن الأقواس، وكل استيراد
   `com.msa.android.*` بيحلّ لتعريف موجود.

## اللي لسه ناقص

- `hostName` في `Constant.swift:26` لسه `http://127.0.0.1:8000/api/v1/`
  — **لازم يبقى `https://api.msagold.com/api/v1/` قبل أي تجربة على جهاز.**
- بناء فعلي على Xcode وAndroid Studio.
- شاشة «نقاطي» في التطبيقين (الباك اند جاهز من شغل سابق).
