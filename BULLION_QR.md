# أكواد QR للسبائك — إثبات أصالة

العميل بيمسح الكود اللي على السبيكة، والتطبيق بيقوله هي مسجّلة عندنا
ولا لأ.

---

## ⚠️ الكود بيتولّد من اللوحة، مش من تطبيق مسح

الأكواد بتتولّد في لوحة التحكم، بتتطبع ورقة استيكرات، والاستيكر بيتلزق
على السبيكة.

يعني الكود موجود في قاعدة بياناتنا **قبل** ما يوصل للسبيكة أصلاً. مفيش
لحظة يكون فيها استيكر مطبوع ومش متسجّل عندنا، ومفيش خطوة «تسجيل»
منفصلة ممكن موظف ينساها — ودي كانت هتبقى المشكلة الأساسية لو التسجيل
كان بالمسح.

## الكود إثبات أصالة، مش تذكرة

**مفيش حالة «مستخدم»**. العميل بيمسح سبيكته كل ما يحب، والمشتري التاني
لازم يقدر يتأكد كمان — ده الغرض من الحكاية كلها. لو خلّيناه مسح واحد،
العميل اللي بيطمّن نفسه تاني مرة كان هيشوف «مرفوض» على سبيكة سليمة.

الحالة الوحيدة اللي بترفض هي `void` — للأكواد اللي اتطبعت وضاعت أو
اتخربت. ودي رسالتها مختلفة عن «مش عندنا» عن قصد: «مالقيناهوش» معناها
يمكن مش بتاعنا، و«اتلغى» معناها كان بتاعنا وفيه حكاية.

---

## اللي جوّه الـQR رابط مش كود

```
https://api.msagold.com/v/ABCDEFGHJKMN
```

لو حطّينا الكود المجرّد، اللي بيمسح بكاميرا الموبايل العادية (وده أغلب
الناس) كان هيشوف نص زي `ABCDEFGHJKMN` ومش هيعرف يعمل بيه إيه. الرابط
بيوديه على صفحة تحقّق على طول، **من غير ما ينزّل التطبيق**.

والتطبيق بيمسح نفس الـQR عادي: `BullionCode::normalize()` بتطلّع الكود
من الرابط.

### التطبيع بيقبل كل الأشكال

| اللي بيوصل | من فين |
|---|---|
| `https://api.msagold.com/v/ABCDEFGHJKMN` | الكاميرا |
| `ABCD-EFGH-JKMN` | موظف بيكتب من الاستيكر |
| `abcdefghjkmn` | حروف صغيرة |
| `  ABCDEFGHJKMN  ` | نسخ ولزق |

كلهم بيرجّعوا نفس الكود. من غير ده، سبيكة سليمة كانت بترجع «مش عندنا»
بسبب شرطة — وده أسوأ عطل ممكن في الفيتشر.

### شكل الكود

١٢ حرف من `ABCDEFGHJKMNPQRSTUVWXYZ23456789` — من غير `0 O 1 I L` لأنهم
بيتلخبطوا لما حد يقراهم من استيكر ويكتبهم. ٣١^١٢ ≈ ٧٫٩ × ١٠^١٧ توليفة،
والتوليد بـ`random_int` (آمن تشفيرياً) — مين يقدر يتوقّع الأكواد يقدر
يطبع استيكر لسبيكة مضروبة.

---

## المسارات

```
POST  api/v1/bullion-codes/verify     التطبيقين — throttle 30/دقيقة
GET   /v/{code}                        صفحة عامة — throttle 30/دقيقة

admin/bullion-codes                    القايمة + فورم التوليد
admin/bullion-codes/print?batch=…      ورقة الاستيكرات
admin/bullion-codes/scans              سجل كل المسحات
admin/bullion-codes/{id}               تفاصيل كود + سجله
```

الرد بيرجع **٢٠٠ دايماً** حتى للكود المجهول — ده نتيجة تحقّق مش خطأ في
الطلب. لو رجّعنا ٤٠٤، كل تطبيق كان هيضطر يفرّق بين «الكود مش موجود»
و«الشبكة وقعت» من نفس الفرع، وأسهل نتيجة لده زرار بيقول «حصل خطأ»
لعميل واقف في المحل بيسأل عن سبيكته.

```json
{ "data": {
    "status": "valid",           // valid | void | unknown
    "valid": true,
    "title": "سبيكة أصلية ✓",
    "message": "الكود ده مسجّل عندنا، والسبيكة من MSA جولد.",
    "bullion": { "code": "ABCD-EFGH-JKMN", "karat": 24, "weight_grams": 10 }
} }
```

النصوص **من السيرفر**، مش مكتوبة في التطبيقات. تعديل الصياغة بينزل مرة
واحدة، والعميل بيشوف نفس الكلام سواء مسح بأندرويد أو iOS أو بكاميرا
الموبايل. الاستثناء الوحيد نص «فشل الاتصال» — ده محلي لأن السيرفر أصلاً
مش موجود ساعتها.

**تفاصيل السبيكة بترجع للكود السليم بس.** المجهول والملغي بيرجّعوا
`null` — مين بيجرّب أكواد عشوائية ما يعرفش أي حاجة عن اللي عندنا.

---

## ⚠️ قبل ما تطبع أول ورقة

**`APP_URL` في `.env` لازم يبقى الدومين الحقيقي.**

الرابط اللي فيها هو اللي بيتطبع جوّه كل كود QR ويتلزق على سبيكة دهب
**للأبد**. لو السيرفر شغّال بإعداد تطوير، هتطبع استيكرات فيها
`http://127.0.0.1:8000/v/CODE` — رابط ما بيفتحش عند أي عميل، وغلط
ما بيتصلّحش بتعديل كود بعدين.

عشان كده **صفحة الطباعة بترفض تشتغل** لو `APP_URL` محلي وبتعرض تحذير
أحمر بدل الاستيكرات. فيه اختبار بيحرس ده
(`test_printing_is_blocked_while_the_app_url_is_local`).

```bash
APP_URL=https://api.msagold.com
php artisan config:clear
```

---

## سجل المسح

كل محاولة بتتسجّل — **حتى الفاشلة**. الأكواد المجهولة هي أهم صفوف في
الجدول: منها بتعرف إن حد بيجرّب أكواد عشوائية، أو إن استيكر اتطبع وما
اتسجّلش عندنا. لو سجّلنا الناجح بس، النوعين دول بيختفوا تماماً.

**معاينة الرابط مش مسحة.** أول ما العميل يلزق الرابط في واتساب، سيرفر
واتساب بيفتحه عشان يعمل معاينة. بنفرّق بهيدر `Sec-Fetch-Dest`، وإلا
عدّاد «اتمسحت كام مرة» كان بيبقى رقم مالوش أي معنى — وأي صفحة كانت تقدر
تحط `<img src="/v/CODE">` وتزوّده على كيفها.

---

## التطبيقين

**أندرويد** كان فيه سكانر شغّال بيدوّر في مجموعة `qr_codes` على
Firestore — **والمجموعة دي مش موجودة في المشروع** (اللي موجود اسمه
`qrs`). يعني كل مسح كان بيرجّع «Invalid» مهما كانت السبيكة. اتحوّل
للـAPI.

وكان بيكتب `isUsed=true` مع كل مسح ناجح (مع إنه ما كانش بيفحصها أبداً).
اتشال — الكود مش تذكرة.

**iOS** مكانش فيه أي سكانر خالص. اتعمل من الصفر: `AVFoundation`، صف في
«المزيد»، ونفس الرسايل بالحرف.

---

## الملفات

```
database/migrations/2026_01_01_000016_create_bullion_codes_tables.php
app/Models/BullionCode.php · BullionCodeScan.php
app/Services/BullionCodeService.php            التوليد والتحقّق — مكان واحد
app/Http/Controllers/Api/BullionCodeController.php
app/Http/Controllers/Shop/VerifyBullionController.php
app/Http/Controllers/Admin/BullionCodeController.php
resources/views/admin/bullion-codes/{index,print,show,scans}.blade.php
resources/views/shop/verify.blade.php
public/js/qrcode.js                            مكتبة QR (MIT) — متسلّمة
tests/Feature/BullionCodeTest.php              ٢٣ اختبار
```

**أندرويد**

```
domain/model/BullionVerification.kt
data/repository/QRRepositoryImpl.kt            بقى بيضرب API مش Firestore
presentation/screens/qr/{ScanQRViewModel,QRScannerScreen,QRCodeAnalyzer}.kt
```

**iOS**

```
MVP/Scenes/MetalsScenes/BullionScanView.swift  جديد كامل
MVP/Scenes/MetalsScenes/MoreVC+MSAExtraRows.swift
MVP/Core/Info.plist                            نص إذن الكاميرا
```

### ليه الـQR بيترسم في المتصفح؟

الطباعة بتحصل من المتصفح أصلاً، والـSVG بيطلع حاد مهما كانت دقة
الطابعة — على عكس PNG بمقاس ثابت. وبكده مفيش مكتبة صور على السيرفر ولا
خطوة توليد ملفات. المكتبة (٥٧ ك.ب، رخصة MIT) متسلّمة في المستودع عشان
الورقة ترسم حتى لو الشبكة الخارجية محجوبة — نفس سبب شيل Tailwind CDN.

**الاستيكر أبيض والكود أسود ومش قابل للتفاوض.** قارئات الـQR بتعتمد
على التباين؛ كود ذهبي على خلفية غامقة «بهوية المحل» شكله حلو ومش
بيتقرا، خصوصاً من كاميرا قديمة أو استيكر عليه بصمات.

---

## اللي لسه مفتوح

- **`TRUSTED_PROXIES` مش متظبّطة في المشروع.** ورا nginx أو Cloudflare،
  `$request->ip()` بترجّع عنوان البروكسي لكل الطلبات — يعني الـthrottle
  بيحسب كل الناس على دلو واحد (٣٠ في الدقيقة **للإنترنت كله**)، وعمود
  الـIP في سجل المسح بيبقى قيمة واحدة مكرّرة. ده إعداد بنية تحتية
  موجود من قبل الفيتشر ده وبيأثر على المشروع كله.
- **`serial` بيتعرض في ٥ أماكن ومفيش طريقة تكتبه.** العمود موجود
  والواجهات بتقراه، بس فورم التوليد مافيهوش خانة ومفيش مسار تعديل. نفس
  الكلام على تصحيح `karat`/`weight_grams` بعد التوليد — الكود الغلط
  ينفع يتلغى بس مينفعش يتصلّح.
- **`hostName` في iOS `Constant.swift:26` لسه `http://127.0.0.1:8000`**
  بينما أندرويد على `https://api.msagold.com`. iOS مش هيكلّم الإنتاج
  زي ما هو.
- **ترحيل مجموعة `qrs` القديمة من Firestore** — لو فيها أكواد شغّالة
  عند عملاء، محتاجة تتنقل للجدول الجديد.
