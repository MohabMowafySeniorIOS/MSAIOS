# إشعارات التطبيق من لوحة التحكم

المشرف بيكتب العنوان والنص بالعربي والإنجليزي، يختار الجمهور، ويدوس
إرسال — والإشعار يوصل الموبايل.

```
admin/push          سجل اللي اتبعت
admin/push/create   فورم الإرسال
admin/push/{id}     تفاصيل إشعار
```

---

## ⚠️ قبل أول إرسال

**`FCM_PROJECT_ID` و`FCM_CREDENTIALS` لازم يكونوا في `.env`.**

من غيرهم المشروع بيستخدم `LogFcmSender` — بيكتب الإشعار في لوج السيرفر
وبيرجّع «نجح». يعني اللوحة بتقول «اتبعت» ومحدش استلم أي حاجة، ومفيش
رسالة خطأ تدور وراها. صفحة الإرسال بتعرض تحذير أحمر لو الإعدادات ناقصة،
وبتفحص **الملف نفسه** مش الإعداد بس.

```bash
FCM_PROJECT_ID=bank-rates-app
FCM_CREDENTIALS=storage/app/firebase/service-account.json
php artisan config:clear
```

الملف من Firebase Console ← Project settings ← Service accounts ←
Generate new private key.

---

## يوصل لمين؟

| الاختيار | الطريقة | بيوصل لمين |
|---|---|---|
| كل اللي نزّلوا التطبيق | توبيك | أي حد فاتح التطبيق، حتى لو مش مسجّل دخول |
| المستخدمين المسجّلين | توكنات | اللي عندنا توكن جهازه في الجدول |
| أندرويد بس / iOS بس | توكنات | نفس اللي فوق مفلتر بالمنصة |

### الفرق مش في العدد بس

**الإرسال للكل** بيمشي على توبيك `general_ar` — وده نفس توبيك
«الإشعارات العامة» الموجود في شاشة إعدادات التطبيق. يعني المستخدم اللي
قافل النوع ده من الإعدادات **مش هيستلم**، وده مقصود: توبيك جديد خاص
باللوحة كان معناه إشعارات ما ينفعش حد يوقفها.

**الإرسال بالتوكنات** مش بيمر على السويتش ده أصلاً، لأن السويتش شغّال
بالاشتراك في التوبيك مش بحاجة عندنا.

### ليه الإرسال للكل مالوش عدد

FCM ما بيرجّعش كام جهاز استلم لما الإرسال يبقى على توبيك — هو نفسه
ما يعرفش وقت الإرسال. فالأعداد بتتسجّل فاضية واللوحة بتكتب «كل
المشتركين». محتاج رقم؟ ابعت لـ«المستخدمين المسجّلين».

---

## اللغة — وليه فيه شرط غريب

كل مستخدم بيستقبل بلغته: الإرسال بالتوكنات بيتقسّم على `users.locale`،
والإرسال العام بيروح مرتين.

بس المرة التانية **مش** على `general_en` عادي:

```
العربي:     topic     = 'general_ar'
الإنجليزي:  condition = 'general_en' in topics && !('general_ar' in topics)
```

السبب إن كل النسخ المنزّلة دلوقتي مشتركة في **التوبيكين مع بعض**، فبعت
رسالتين عاديتين كان بيوصل لنفس الجهاز مرتين — واحدة عربي وواحدة
إنجليزي. الشرط بيخلي الجهاز المشترك في الاتنين ياخد العربي بس.

بعد ما المستخدمين يحدّثوا التطبيق (النسخة الجديدة بتشترك في لغة
المستخدم بس وبتلغي التانية صراحةً) الإنجليزي بيوصل لأصحابه صح.

> **نفس المشكلة موجودة في إشعارات الفيدرالي.** `FomcNotifier` بيبعت على
> `fomc_ar` و`fomc_en`، فالمستخدم بياخد الإشعار مرتين دلوقتي فعلاً.
> الإصلاح سطرين بنفس الطريقة — محتاج قرارك.

---

## التوكنات

### المسار الجديد

```
POST /api/v1/device-token        محتاج توكن دخول
{ fcm_token, device_platform, app_version, locale }
```

التوكن كان بيتبعت مع التسجيل والدخول بس. بس FCM بيجدّده لوحده (تحديث
التطبيق · استعادة نسخة احتياطية · مسح بيانات التطبيق)، والمستخدم ممكن
يفضل شهور من غير دخول تاني — فالتوكن اللي عندنا بيبوظ والإشعار الموجّه
بيروح في الهوا. والتطبيقين دلوقتي بينادوا المسار ده عند فتح التطبيق،
وعند تجديد التوكن، وبعد الدخول، وبعد تغيير اللغة.

التوكن بيخص **جهاز مش حساب**: لو حد سجّل خروج وحد تاني دخل على نفس
الموبايل، المسار بيشيل التوكن من الحساب الأول — وإلا الإشعار الموجّه
للأول كان بيوصل للتاني وهو فاتح التطبيق.

### تنضيف التوكنات الميتة

لما FCM يقول إن توكن مات (التطبيق اتشال)، بنصفّره من الجدول. من غير ده
الجدول بيمتلي بتوكنات بتاعة ناس مش موجودة، وكل إرسال بيبقى أبطأ و«وصل
لـ٥٠٠» بتبقى رقم مالوش علاقة بالواقع.

**وفيه صمام أمان.** FCM v1 مالوش نداء جماعي — كل توكن طلب لوحده، يعني
أي عطل عام (`FCM_PROJECT_ID` غلط · صلاحية ناقصة · الخدمة واقعة) بيرجّع
نفس الخطأ لكل التوكنات مع بعض. من غير حماية، غلطة سطر واحد في `.env`
كانت بتصفّر العمود للجدول كله. حاجتين بيمنعوا ده:

1. الحكم بإن التوكن مات بيتاخد من **جسم الرد** (`errorCode: UNREGISTERED`)
   مش من كود HTTP. مشروع غلط بيرجّع `404 NOT_FOUND` بالظبط زي التوكن
   الميت — الفرق إن الميت بيجيب `details` فيها `UNREGISTERED`.
2. لو أكتر من نص المستهدفين طلعوا «ميتين» في إرسال واحد، التنضيف بيتلغي
   وبيتسجّل في اللوج. ده مش توكنات ميتة، ده عطل.

---

## حاجات في الإرسال نفسه

**الإرسال بيحصل جوّه طلب الصفحة.** مفيش قوائم انتظار في المشروع
(`QUEUE_CONNECTION=sync` ومفيش جداول jobs)، فالإرسال بالتوكنات بيتم
بالتوازي — ٥٠ طلب مع بعض — عشان المشرف ما يقعدش مستني. لعدد مستخدمين
كبير (عشرات الآلاف) ده هيحتاج ينتقل لطابور حقيقي.

**الصف بيتكتب بحالة `sending` قبل الإرسال** مش `sent`. لو الطلب مات في
النص، السجل بيفضل مكتوب عليه إنه وقف — مش إنه نجح.

**حارس التكرار**: نفس العنوان لنفس الجمهور مش هيتبعت تاني خلال دقيقتين.
الإشعار ما بيترجعش، وأول رد فعل على صفحة واقفة إنك تدوس تاني.

**الطول محدود** (١٠٠ للعنوان · ٢٥٠ للنص). FCM بيقبل أطول، بس نظام
الإشعارات في الموبايل بيقص — والمشرف كان هيفتكر إن الفقرة وصلت كاملة.

---

## الملفات

```
app/Services/FcmSender.php                    الواجهة (+ sendToCondition و sendToTokens)
app/Services/HttpFcmSender.php                FCM HTTP v1 — التنفيذ الحقيقي
app/Services/LogFcmSender.php                 بديل التطوير
app/Services/PushBroadcastService.php         منطق الإرسال — مكان واحد
app/Models/PushNotification.php
app/Http/Controllers/Admin/PushNotificationController.php
app/Http/Controllers/Api/DeviceTokenController.php
database/migrations/2026_01_01_000017_create_push_notifications_table.php
resources/views/admin/push/{create,index,show,_status}.blade.php
tests/Feature/PushNotificationTest.php        ٣١ اختبار
tests/Feature/HttpFcmSenderTest.php           ١٢ اختبار على المُرسِل الحقيقي
```

**أندرويد**

```
data/source/local/NotificationPreferences.kt        الاشتراك بلغة المستخدم
data/repository/DeviceRepositoryImpl.kt             مزامنة التوكن
domain/repository/Repositories.kt · di/RepositoryModule.kt
presentation/screens/language/LanguageViewModel.kt
presentation/screens/auth/AuthViewModel.kt · data/repository/AuthRepositoryImpl.kt
presentation/service/MSAFirebaseMessagingService.kt · MSAApplication.kt
data/source/network/MsaApi.kt · dto/AuthDtos.kt
```

**iOS**

```
MVP/Scenes/MetalsScenes/NotificationSettings.swift  الاشتراك بلغة المستخدم
MVP/Scenes/Auth/DeviceTokenSync.swift               جديد
MVP/Scenes/Auth/AuthAPI.swift · AuthViewModel.swift
MVP/Core/AppDelegate.swift                          إصلاح الكراش + التوكن
MVP/LocalizationFiles/L10language.swift             تشديد قراءة اللغة
MVP/Extensions/.../UIViewController+Extentions.swift تغيير اللغة
```

---

## إصلاحات دخلت مع الشغل ده

**iOS كان بيقفل لما يوصله إشعار بشكل مختلف.** `AppDelegate` كان بيعمل
`as!` على `aps` وعلى `aps["alert"]`. الإشعار مش لازم يكون فيه `aps`،
و`alert` ممكن تيجي **نص** مش قاموس. مكانش باين لأن الإشعارات كانت
بتتبعت من مكان واحد بشكل ثابت — أول إشعار من اللوحة كان ممكن يولّع.

**التطبيقين كانوا بياخدوا كل إشعار مرتين** (لما السيرفر يبعت باللغتين)
لأنهم مشتركين في `_ar` و`_en` مع بعض. اتظبط في التطبيقين + الشرط في
السيرفر للنسخ القديمة.

**أندرويد كان بيبعت `locale: "ar"` ثابتة** عند التسجيل مهما كانت لغة
المستخدم. يعني السيرفر كان فاكر إن كل مستخدمي أندرويد عرب.

**iOS كان بيبعت اللغة الخام** عند التسجيل، فجهاز بالأوردو كان بيترفض.

---

## ⚠️ حاجات لسه مفتوحة

- **`hostName` في iOS `Constant.swift:26` لسه `http://127.0.0.1:8000`**
  بينما أندرويد على `https://api.msagold.com`. كل نداءات الحساب — ومنها
  مزامنة التوكن — مش بتخرج من الجهاز أصلاً. **الفيتشر ده مش هيشتغل على
  iOS قبل ما ده يتظبط.**
- **iOS بيرجّع اللغة للعربي إجبارياً عند كل فتح.**
  `AppDelegate.swift:105` بيقول `if Helper.getisFirst() != true` —
  و`SaveisFirst` بيتنادى من `OnBoardingVC` بس، اللي مش موصول في المسار
  الحالي (الفرع متعلّق عليه تعليق في `SplashVC.handlenavigation`). يعني
  الشرط دايماً صح. ده عطل موجود من قبل الشغل ده، بس معناه إن نسخة iOS
  الإنجليزية عملياً مش شغّالة — والإشعار الإنجليزي مش هيوصل لحد على
  iOS قبل ما يتصلّح. الإصلاح سطر: يتفحص `getisStart_Language()` بدل
  `getisFirst()`.
- **`TRUSTED_PROXIES` مش متظبّطة.** ورا nginx أو Cloudflare الـthrottle
  بيحسب كل الناس على دلو واحد. موجودة من قبل ده وبتأثر على المشروع كله.
- **أي مشرف يقدر يبعت للقاعدة كلها.** `Admin::isSuperAdmin()` موجودة في
  الموديل ومش مستخدمة في أي مكان في المشروع. لو عايز الإرسال يتقفل على
  مشرفين معيّنين، ده أول استخدام ليها.
